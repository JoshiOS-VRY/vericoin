global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: binarychain-observer
    static_configs:
      - targets:
          - observer:9101
        labels:
          network: binarytest
